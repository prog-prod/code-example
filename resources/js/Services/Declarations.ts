declare const route: (
    // eslint-disable-next-line no-unused-vars
    name: string,
    // eslint-disable-next-line no-unused-vars
    params?: any,
    // eslint-disable-next-line no-unused-vars
    absolute?: boolean
) => string;

export default route;
